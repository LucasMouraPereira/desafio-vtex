import type { ComponentType } from 'react';
export declare function withSettings(WrappedComponent: ComponentType<{
    settings?: Settings;
}>): (props: any) => JSX.Element;
export interface Settings {
    showGenders: boolean;
    showMyCards: boolean | null;
    showMyOrders: boolean | null;
    useMap: boolean;
}

import type { ComponentType } from 'react';
export declare const withContentWrapper: ({ headerConfig, handle, }: {
    headerConfig: ContentWrapperProps;
    handle?: {
        contentHandle: string;
        configHandle: string;
    } | undefined;
}) => (WrappedComponent: ComponentType<InjectedContentWrapperProps>) => (props: {
    [key: string]: unknown;
}) => JSX.Element;

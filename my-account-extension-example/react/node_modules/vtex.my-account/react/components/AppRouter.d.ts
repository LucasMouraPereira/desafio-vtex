import type { FC, ComponentClass } from 'react';
import { Component } from 'react';
interface Props {
    blockDocument?: boolean;
}
declare class AppRouter extends Component<Props> {
    state: {
        defaultPath: string;
    };
    private handleDefaultPath;
    static getSchema: () => {
        title: string;
        description: string;
        type: string;
        properties: {
            defaultPath: {
                title: string;
                type: string;
                default: string;
            };
        };
    };
    mapRouteComponent: ({ path, component, }: {
        path: string;
        component: ComponentClass<void, unknown> | FC;
    }) => JSX.Element;
    render(): JSX.Element;
}
export default AppRouter;
